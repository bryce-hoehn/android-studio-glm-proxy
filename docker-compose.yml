services:
  android-studio-llm-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: android-studio-glm-proxy
    ports:
      - "8000:8000"
    environment:
      - API_KEY=${API_KEY}
    env_file:
      - .env
    restart: unless-stopped